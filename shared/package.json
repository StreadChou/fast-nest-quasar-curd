{
  "name": "shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "patch": "build && npm version patch --no-git-tag-version",
    "minor": "build && npm version minor --no-git-tag-version",
    "major": "build && npm version major --no-git-tag-version",
    "clean": "rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "typeorm": "^0.3.20",
    "reflect-metadata": "^0.2.0"
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}
